services:
  cardduel-content-studio:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:5000}
    ports:
      - "5173:80"
    environment:
      - TZ=America/Santiago
